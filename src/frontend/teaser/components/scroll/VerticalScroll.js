import { ScrollView } from 'react-native';

/**
 * Vertical scroll between teasers.
 * @returns 
 */
export default function VerticalScroll()    {
    return (
        <ScrollView>
        </ScrollView>
    );
}
