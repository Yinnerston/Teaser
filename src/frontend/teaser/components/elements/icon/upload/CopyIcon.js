import { Ionicons } from "@expo/vector-icons";
import { HOME_NAV_ICON_SIZE } from "../../../../Constants";

export default function CopyIcon() {
  return <Ionicons name="ios-copy" size={HOME_NAV_ICON_SIZE} color="white" />;
}
