import { Ionicons } from "@expo/vector-icons";
import { HOME_NAV_ICON_SIZE } from "../../../../Constants";
export default function TrashIcon() {
  return <Ionicons name="trash" size={HOME_NAV_ICON_SIZE} color="white" />;
}
