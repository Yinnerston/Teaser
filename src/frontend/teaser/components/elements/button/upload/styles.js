import { StyleSheet } from "react-native";
export const editorSidebarStyles = StyleSheet.create({
  buttonContainerStyle: {
    justifyContent: "center",
    alignItems: "center",
  },
  videoEditorText: {
    color: "white",
  },
});
