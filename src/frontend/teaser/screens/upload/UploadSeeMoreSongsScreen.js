import { View, Text, Button } from "react-native";

export default function UploadSeeMoreSongsScreen({ navigation }) {
  return (
    <View>
      <Text>Not Implemented</Text>
      <Button title="Go Back" onPress={() => navigation.goBack()}>
        Go Back
      </Button>
    </View>
  );
}
